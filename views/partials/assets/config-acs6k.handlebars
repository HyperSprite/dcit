
{{#each systemPorts}}
{{#if sysPortisConsole}}
  <div class="col-md-6 ">
  <h5>Avocent Console Config</h5>
  <button class="btn btn-xs btn-danger" data-clipboard-target="#conConfigCode">Copy</button>
    <blockquote>
    <ol>
      <li>SSH to console server <strong>{{sysPortEndPoint}}</strong> as a privileged user. </li>
      <li>From the cli-> prompt, paste the following commands.</li>
    </ol>
      <code id="conConfigCode">
      cd /ports/serial_ports<br>
      {{#if sysPortEndPointPort}}set_cas <strong>{{sysPortEndPointPort}}</strong>{{else}}<mark>set_cas ??</mark>{{/if}}<br>
      {{#if sysPortOptions}}set enable_cisco_rj45_pin-out=<strong>{{sysPortOptions}}</strong>{{else}}<mark>set enable_cisco_rj45_pin-out=??</mark>{{/if}}<br>
      set status=enabled<br>
      set speed=9600<br>
      set parity=none<br>
      set data_bits=8<br>
      set stop_bits=1<br>
      set flow_control=none<br>
      cd cas<br>
      set port_name=<strong>{{../../systemName}}</strong><br>
      set enable_auto_discovery=no<br>
      set protocol=ssh<br>
      set authentication_type=ldap|local<br>
      save --cancelOnError<br>
      echo on<br>
      commit<br>
      </code>

    </blockquote>

  </div>

{{/if}}
{{/each}}

